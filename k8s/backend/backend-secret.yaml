# ============================================
# Backend Secret
# 存储后端敏感信息（Django 密钥、数据库密码等）
# ============================================
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: login-system
  labels:
    app: backend
    tier: application
type: Opaque
data:
  # Django 密钥 (随机生成，生产环境请更换)
  # django-secret-key-change-in-production-environment
  # echo -n 'django-secret-key-change-in-production-environment' | base64
  SECRET_KEY: ZGphbmdvLXNlY3JldC1rZXktY2hhbmdlLWluLXByb2R1Y3Rpb24tZW52aXJvbm1lbnQ=
  
  # 数据库密码 (postgres123)
  # echo -n 'postgres123' | base64
  DB_PASSWORD: cG9zdGdyZXMxMjM=

